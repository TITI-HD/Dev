name: 🔍 Surveillance WordPress.com

on:
  schedule:
    - cron: "*/30 * * * *"   # Vérifie toutes les 30 minutes
  workflow_dispatch:          # Exécution manuelle possible

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Récupération du dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 📦 Installer les dépendances
        run: pip install requests

      - name: 🚀 Exécuter le script de surveillance
        env:
          SITE_URL: ${{oupssecuretest.wordpress.com}}
          ALERT_EMAIL: ${{ secrets.danieltiti882@gmail.com}}
          SMTP_SERVER: ${{ secrets.danieltiti882@gmail.com}}
          SMTP_PORT: ${{ secrets.587}}
          SMTP_USER: ${{ secrets.danieltiti882@gmail.com}}
          SMTP_PASS: ${{ secrets.osmp bmyl hzhd gtvz}}
        run: python monitor.py