<<<<<<< HEAD
name: 🛡️ CI/CD WordPress.com

on:
  schedule:
    - cron: '0 */6 * * *'  # Toutes les 6 heures
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration de Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Installation des dépendances
        run: pip install -r requirements.txt

      - name: 🔒 Scan de sécurité Bandit
        run: |
          pip install bandit
          bandit -r . -f html -o security-report.html || true

      - name: 📊 Upload du rapport de sécurité
        uses: actions/upload-artifact@v4  # Changé de v3 à v4
        with:
          name: security-report
          path: security-report.html

  monitor:
    runs-on: ubuntu-latest
    needs: security-scan
    environment: production
    
    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration de Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Installation des dépendances
        run: pip install requests twilio python-gnupg  # Ajout de python-gnupg

      - name: 🚀 Exécution de la surveillance
        env:
          SITE_URL: ${{ vars.SITE_URL }}
          ALERT_EMAIL: ${{ secrets.ALERT_EMAIL }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_WHATSAPP_FROM: ${{ secrets.TWILIO_WHATSAPP_FROM }}
          TWILIO_WHATSAPP_TO: ${{ secrets.TWILIO_WHATSAPP_TO }}
        run: python monitor.py

  backup:
    runs-on: ubuntu-latest
    needs: monitor
    environment: production
    
    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration de Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Installation des dépendances
        run: pip install requests python-gnupg  # Ajout de python-gnupg

      - name: 💾 Exécution de la sauvegarde
        env:
          SITE_URL: ${{ vars.SITE_URL }}
        run: python backup.py  # Changé de backup_script.py à backup.py

      - name: 📤 Archivage des sauvegardes
        uses: actions/upload-artifact@v4
        with:
          name: wordpress-backup-${{ github.run_id }}
          path: backups/
          retention-days: 7
          if-no-files-found: error
=======
name: 🛡️ Surveillance WordPress  # Nom : Surveillance WordPress.

on:  # Triggers.
  schedule:  # Planifié.
    - cron: '0 */6 * * *'  # Toutes les 6 heures.
  workflow_dispatch:  # Manuel.

jobs:  # Jobs.
  monitor:  # Job monitor.
    runs-on: ubuntu-latest  # Ubuntu.
    
    steps:  # Steps.
      - name: 📥 Récupération du dépôt  # Step : Checkout.
        uses: actions/checkout@v3  # v3 (note : plus ancien que v4).

      - name: 🐍 Installer Python  # Step : Setup Python.
        uses: actions/setup-python@v4  # v4.
        with:  # Params.
          python-version: "3.11"  # Version 3.11.

      - name: 📦 Installer les dépendances  # Step : Install deps.
        run: |  # Commandes multi.
          python -m pip install --upgrade pip  # Upgrade pip.
          pip install requests twilio  # Install spécifiques (pas requirements.txt ?).

      - name: 🚀 Exécuter la surveillance  # Step : Run monitor.
        env:  # Env secrets Twilio.
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}  # SID.
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}  # Token.
          TWILIO_WHATSAPP_FROM: ${{ secrets.TWILIO_WHATSAPP_FROM }}  # From.
          TWILIO_WHATSAPP_TO: ${{ secrets.TWILIO_WHATSAPP_TO }}  # To.
        run: python monitor.py  # Exécute monitor.py.

  security-scan:  # Job scan (dépend de monitor).
    runs-on: ubuntu-latest  # Ubuntu.
    needs: monitor  # Dépend de monitor.
    if: always()  # Toujours exécuté.

    steps:  # Steps.
      - name: 📥 Récupération du dépôt  # Checkout.
        uses: actions/checkout@v3  # v3.

      - name: 🐍 Installer Python  # Setup Python.
        uses: actions/setup-python@v4  # v4.
        with:  # Params.
          python-version: "3.11"  # 3.11.
>>>>>>> f187b1211e2b27bf3d01b368312f0f2bba2b0874
